search for a record and return the pk
write all need querys
scrap the text file for necessary information and import it
finish schema
finish import lists
finish weekly import script
write script to detect if I own a comic and update reading list if so
